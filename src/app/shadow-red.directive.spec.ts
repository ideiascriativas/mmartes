import { ShadowRedDirective } from './shadow-red.directive';

describe('ShadowRedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowRedDirective();
    expect(directive).toBeTruthy();
  });
});
