import { RainbowEffectDirective } from './rainbow-effect.directive';

describe('RainbowEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowEffectDirective();
    expect(directive).toBeTruthy();
  });
});
