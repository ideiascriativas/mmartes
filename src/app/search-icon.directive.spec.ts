import { SearchIconDirective } from './search-icon.directive';

describe('SearchIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchIconDirective();
    expect(directive).toBeTruthy();
  });
});
