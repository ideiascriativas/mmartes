import { BackImageDirective } from './back-image.directive';

describe('BackImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BackImageDirective();
    expect(directive).toBeTruthy();
  });
});
