import { IconMenuDirective } from './icon-menu.directive';

describe('IconMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new IconMenuDirective();
    expect(directive).toBeTruthy();
  });
});
