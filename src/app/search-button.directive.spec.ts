import { SearchButtonDirective } from './search-button.directive';

describe('SearchButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchButtonDirective();
    expect(directive).toBeTruthy();
  });
});
